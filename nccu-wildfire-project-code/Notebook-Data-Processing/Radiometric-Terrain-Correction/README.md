RTC_notebook should be run first to extract the rtc files for each flight line you are working with, only when extracting the polarization values from the flight lines, and not for the alpha 1 angles. 

Then the Incidence angle notebook should be run to extract the headers for the .inc files to be later used in the merging step of the data processing. 
